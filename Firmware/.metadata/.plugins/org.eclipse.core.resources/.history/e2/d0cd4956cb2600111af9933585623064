/*
 * mouvement.h
 *
 *  Created on: Mar 12, 2026
 *      Author: lucasa
 */


#ifndef INC_MOUVEMENT_H_
#define INC_MOUVEMENT_H_

#include "asservissement.h"
#include <math.h>
#include <stdlib.h>
#include <stdbool.h>


#define DISTANCE_ROUE 87.0f

typedef enum {STOP,AVANCER,TOURNER} ETAT;

typedef struct mouvement{
    ETAT etat;
    int32_t distance_ticks_cible;
    int32_t distance_ticks_g_parcourue;
    int32_t distance_ticks_d_parcourue;
    float vitesse_ticks_ms;
    bool angle_positif;
} MOUVEMENTCONTROL;

void move_init(MOUVEMENTCONTROL * mvt);
void avancer(float distance, float vitesse);
void tourner(float angle_deg, float vitesse);
void stop(void);

void mouvement_update(void);

#endif
